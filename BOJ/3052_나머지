#include <stdio.h>
#pragma warning (disable:4996)

int main(void)
{
    int n[11] = { 0 };
    int result = 0;

    for (int i = 0; i < 10; i++)
    {
        scanf("%d", &n[i]);
        n[i] = n[i] % 42;
    }

    for (int i = 0; i < 10; i++)
    {
        int count = 0;
        for (int j = i + 1; j < 10; j++)
        {
            if (n[i] == n[j])
            {
                count++;
            }
        }
        if (count == 0)
        {
            result++;
        }
    }

    printf("%d", result);
}
